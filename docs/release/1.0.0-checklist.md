# Release checklist — 1.0.0 (GV-21)

Preparation only (`docs/spec/operations/testing.md` "Manual / release checklist",
`docs/spec/operations/release.md`, `docs/spec/operations/compliance.md` "Supply chain and release
integrity"). No tag cut, no branch merged to `production`, no Modrinth publish — Kevin's rulings on
the shrink trade-off and the city depth, and his own client look, come first
(`docs/spec/decisions/DEC-006-size-tiers.md`, `docs/spec/decisions/DEC-010-shrink-move-vanilla.md`).

Run against `mod.version = 1.0.0` (`stonecutter.properties.toml`), commit `1174998` (spec sync) on
`chore/gv-21-1-0-0`, on top of `development` at `93cc507`.

## Headless — run and ticked here

- [x] **`chiseledBuild`, all twelve jars, clean checkout.** `./gradlew clean chiseledBuild
  --continue` — `BUILD SUCCESSFUL`. Twelve jars under `build/libs/1.0.0/`, one per
  `docs/spec/contracts/platform-matrix.md` combination. Names and checksums below.
- [x] **`chiseledCheck`.** Part of `just check`'s `test` stage — `./gradlew chiseledCheck
  --continue`, `BUILD SUCCESSFUL`; `SeedSweepStatsTest: 20/20 passed` on every node that carries
  it (`26.2-fabric`, `1.21.8-fabric`, `26.2-neoforge`, and every other Fabric/NeoForge node).
- [x] **`chiseledGameTest`.** Part of `just check` — `./gradlew chiseledGameTest --continue`,
  `BUILD SUCCESSFUL` (`26.2-fabric`'s `runGameTest`, `1.21.1-neoforge`'s `runGameTestServer`, per
  `docs/spec/operations/testing.md` "V2": NeoForge 26.2 itself has no game-test bridge yet, an
  already-recorded gap, not new to this release — see "Known limitations" in `CHANGELOG.md`).
- [x] **`just doctor`** (`doctor-repo` + `doctor-toolchain`). `kontor doctor`: 10 checks, all
  passed. `python3 tools/doctor.py`: every Java-floor, Gradle-wrapper, Stonecutter-version,
  dependency-coordinate, `targets.json`-coverage, target-jar-name, and game-versions/Java-boundary
  check passed; `docs/spec/` confirmed identical to the vault (post spec-sync commit).
- [x] **`just lint`** (purity-check + `chiseledCheck -x test`). `purity: clean (26 files scanned, 4
  packages, 2 known adapters excepted)`.
- [x] **`just map-check`.** Passed after `just map` regenerated `docs/map.md` for
  `tools/target_notes.py`'s new checksum-handling functions (`load_checksums`, `files_line`,
  `notes_for`/`write_all`'s new `checksums` parameter).
- [x] **`just check`** end to end (`lint map-check test gametest build`). `BUILD SUCCESSFUL`,
  every sub-stage above green in the same run.
- [x] **The harness, one seed, on 26.2-fabric, 1.21.8-fabric, 26.2-neoforge** (GV-10/GV-12/GV-17's
  own combinations), fresh against this release's own build, each its own freely-allocated port
  (`seedSweep`'s own per-run free-port allocation, `docs/baseline/README.md` "The exact command"):

  | Node | Command | Seed 1 result |
  |---|---|---|
  | `26.2-fabric` | `./gradlew :26.2-fabric:seedSweep -Pcount=1` | `village_plains` at `(640, 0, 816)`, 52 pieces, height spread 3.0, water 0.0%, tier `hamlet`, outcome `shrink` (3 water + 3 height rejected) |
  | `1.21.8-fabric` | `./gradlew :1.21.8-fabric:seedSweep -Pcount=1` | identical: same village, same 52 pieces, same 3/3 rejection split, same `shrink`→`hamlet` outcome — cross-Minecraft-version determinism confirmed for the same seed |
  | `26.2-neoforge` | `./gradlew :26.2-neoforge:seedSweep -Pcount=1` | identical again — cross-loader determinism confirmed alongside GV-12's own finding |

  All three match `docs/baseline/grounded-26.2-fabric-10-seeds-all.json`'s own seed-1 row exactly
  (52 pieces, `hamlet`, 3 water + 3 height rejected), the same cross-check
  `docs/spec/contracts/platform-matrix.md`'s "Third correction" section already used for the Forge
  leg's own seed-1 proof.
- [x] **The harness equivalent on `1.20.1-forge`, as GV-15 did.** No `seedSweep` task exists for
  the Forge leg (`build.forge.gradle.kts` defines no such task; GV-10/GV-12's `seedSweep` lives
  only in `build.fabric.gradle.kts`/`build.neoforge.gradle.kts` — a genuine tooling gap, not
  something invented here to paper over it). What stands in as this node's own evidence:
  `docs/spec/contracts/platform-matrix.md`'s "Third correction, live-verified (GV-15, 2026-09-21):
  the Forge 1.20.1 leg's SRG refmap proof" — a real, installer-built, SRG-named production Forge
  1.20.1 server, this mod's jar dropped into `mods/`, booted under Java 17, with
  `-Dgrounded_villages.debug=true`: all three mixins apply
  (`JigsawStructureMixin`/`JigsawPlacementMixin`/`PlacerMixin`), and the full hook chain fires for
  two real seeds, seed 1 matching this same 52-piece/`shrink`/`hamlet` result the three sweeps
  above reproduce on the other loaders. That proof is **the same combination** (not carried over
  from a different one, `GV-21`'s own acceptance criteria) and nothing in this release changes the
  Forge leg's own placement logic — GV-21/GV-24's only change to this node is the jar file name
  (`grounded_villages-1.20.1-forge-1.0.0.jar` in place of the `0.1.0+1.20.1` scheme GV-15 built
  against), reproven fresh in this same run by `:1.20.1-forge:check` and `:1.20.1-forge:buildAndCollect`
  (both part of the `chiseledCheck`/`chiseledBuild` runs above) succeeding on the 1.0.0 jar.
  **Flagged, not silently patched**: a `seedSweep`-equivalent task for the Forge leg is a real gap
  worth a future ticket, the same way `docs/spec/operations/testing.md` already flags the NeoForge
  26.2 game-test gap rather than pretending it doesn't exist.

## The twelve jars and their checksums

`dist/SHA256SUMS` (from `build/libs/1.0.0/`, `shasum -a 256 *.jar`):

| Jar | SHA-256 |
|---|---|
| `grounded_villages-1.20.1-fabric-1.0.0.jar` | `478ed8b64abc952badf8e3c5475e1da6fa78b18ac8b8f4152eefee9d97b22d77` |
| `grounded_villages-1.20.1-forge-1.0.0.jar` | `e60569eaa75f5604c6ec6bd6663b9df155dc2b455a221bfc56d73c1fe0eb7400` |
| `grounded_villages-1.21.1-fabric-1.0.0.jar` | `2b73c71939834a2947f097b9b3af29a5332afaeacaf969073a33d69d8d4ae31b` |
| `grounded_villages-1.21.1-neoforge-1.0.0.jar` | `43fc978f0134bc991f5a93cf4a4e6ab876520fd5b355b79c2282facb325e0008` |
| `grounded_villages-1.21.4-fabric-1.0.0.jar` | `f2f10a7b3fdf5c498160e239ae0317d4a0ba67c9b12e32b7b59e7fd4fe4e7700` |
| `grounded_villages-1.21.4-neoforge-1.0.0.jar` | `9579340aae823ddddd4f70a3fdb310701540ea89c087ff963d6e12b1bdfb6726` |
| `grounded_villages-1.21.5-fabric-1.0.0.jar` | `f4affadfd67ad6142c600a0363130933d9dfa8b4974e4e36c9a08386d42b56a4` |
| `grounded_villages-1.21.5-neoforge-1.0.0.jar` | `df53631f9d67a7dacbb6f83a97718ecd26ede14922f748fa60c5480c4203eaf7` |
| `grounded_villages-1.21.8-fabric-1.0.0.jar` | `48bf48ef2f775db6667c058e295ef9c69ade4abbdad7bb99552906315be97237` |
| `grounded_villages-1.21.8-neoforge-1.0.0.jar` | `4fc55e070fc705200ae54d0963e27bba38ad2433e1e109670efcc3123f6cd422` |
| `grounded_villages-26.2-fabric-1.0.0.jar` | `9824823b0f540629af4ad7a7763589fcd3407ccdb9dc670eea03cb46a900a11e` |
| `grounded_villages-26.2-neoforge-1.0.0.jar` | `3337e7a610dab2adbe11bd6b5afa236e1575961b579bca8bb2d66e7ac3d59fc8` |

Each jar's own checksum is also embedded in its own per-target notes file
(`dist/notes/<version_number>.md`, `just target-notes`/`tools/target_notes.py`), not only in the
combined `SHA256SUMS` list — `operations/compliance.md` "Supply chain and release integrity".

## Client-side — for Kevin, not ticked here

`docs/spec/operations/testing.md`'s "Manual / release checklist" row: generate a fresh test world
per shipped combination and confirm, by eye, several villages each show a consistent ground level,
no water-crossing pieces or streets, and at least one visibly different-sized village across a
reasonable exploration radius. One real client per loader family, `/locate`, and a look — exactly
what a headless run cannot stand in for.

- [ ] `just client 1.20.1-fabric`
- [ ] `just client 1.20.1-forge`
- [ ] `just client 1.21.1-fabric`
- [ ] `just client 1.21.1-neoforge`
- [ ] `just client 1.21.4-fabric`
- [ ] `just client 1.21.4-neoforge`
- [ ] `just client 1.21.5-fabric`
- [ ] `just client 1.21.5-neoforge`
- [ ] `just client 1.21.8-fabric`
- [ ] `just client 1.21.8-neoforge`
- [ ] `just client 26.2-fabric`
- [ ] `just client 26.2-neoforge`

Per client: `/locate structure minecraft:village_plains` (or `village_snowy`/`village_taiga`/
`village_desert`/`village_savanna`) from spawn, teleport or fly out to a couple of results, and
look for: one consistent ground level across the village (no house floating or buried), no street
or building sitting in water, and — across more than one located village — a visible size
difference (a small hamlet next to a full-size village, ideally a rarer town or city).

Also for Kevin, not a headless item: the ruling this ticket's own instructions name as a
precondition for cutting the release — the shrink trade-off (`DEC-010`) and the city depth being a
budget estimate, not a hard bound (`DEC-006`, "Known limitations" in `CHANGELOG.md`).
