<!-- generated by tools/map.py; edit the source, then run `just map` -->

# `grounded_villages.harness` (seedsweep-neoforge) — root

Every type with its summary and every non-private constructor, method and constant. The
signature is the contract; read the source only when the summary is not enough.

### `class SeedSweepCommandNeoForge` — `src/seedsweep-neoforge/java/grounded_villages/harness/SeedSweepCommandNeoForge.java`
GV-12's NeoForge twin of SeedSweepCommand (GV-10, the Fabric leg) -- same dev-only sweep command and the same seedSweep Gradle-task hook into SeedSweepRunner#run, adapted only for NeoForge's own event shape: no ServerLifecycleEvents.SERVER_STARTED (a Fabric API module this project does not depend on for the NeoForge legs), so this class subscribes to net.neoforged.neoforge.event.server.ServerStartedEvent and net.neoforged.neoforge.event.RegisterCommandsEvent on NeoForge#EVENT_BUS instead -- both confirmed present and stable across NeoForge 21.1.251 and 26.2.0.88 by direct decompilation of the real neoforge-*-userdev.jar artifacts (GV-12), unlike the one real API drift GroundedVillagesNeoForge itself has to straddle.
- `void register()` — Called reflectively; see the class doc.
- `void onRegisterCommands(RegisterCommandsEvent event)` — Static @SubscribeEvent methods, registered via NeoForge.EVENT_BUS.register called with a Class (not an instance) in #register above -- confirmed live against the real net.neoforged:bus:8.0.5 jar (GV-12): IEventBus.register accepts either an instance (non-static listeners) or a Class (static listeners), and this event bus's own error message for a static method registered via an instance ("because register() was called with a class type ...
- `void onServerStarted(ServerStartedEvent event)`

