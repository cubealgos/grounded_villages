<!-- generated by tools/map.py; edit the source, then run `just map` -->

# `grounded_villages.gametest` (gametest) — root

Every type with its summary and every non-private constructor, method and constant. The
signature is the contract; read the source only when the summary is not enough.

Ticket GV-11 (`docs/spec/operations/testing.md` "Game tests" row, `TEST-REQ-004`): live, loader-agnostic game-test scenarios proving the mixin hook fires correctly against a known, pre-built scenario -- not the seed-driven statistics `SeedSweepRunner` (GV-10) already owns.

### `class LiveLevelTerrainSampler` — `src/gametest/java/grounded_villages/gametest/LiveLevelTerrainSampler.java`
A TerrainSampler over a game test's own live ServerLevel, backed by a direct column scan of real block state rather than ServerLevel#getHeight's own tracked heightmaps -- the "synthetic TerrainSampler over the test level" ticket GV-11's own build item names.
- `LiveLevelTerrainSampler(ServerLevel level)`
- `int getBaseHeight(int x, int z, Heightmap.Types type)`
- `NoiseColumn getBaseColumn(int x, int z)`
- `int getSeaLevel()`

### `class PieceGateScenarios` — `src/gametest/java/grounded_villages/gametest/PieceGateScenarios.java`
The GV-11 scenario bodies, called from a thin, per-loader @GameTest-annotated method in grounded_villages.fabric.gametest/grounded_villages.neoforge.gametest (both loaders' own game-test framework ultimately hands a real GameTestHelper to a Consumer-shaped method, so one body serves both -- see the package's own javadoc).
- `void waterFootprintIsRejected(GameTestHelper helper)`
- `void dryFootprintIsAccepted(GameTestHelper helper)`
- `void rejectionLeavesNoDanglingConnector(GameTestHelper helper)`
- `void acceptedCandidateIsCommittedAndUnmutated(GameTestHelper helper)`
- `void ladderRollsShrinkAndRetryFromSyntheticCounts(GameTestHelper helper)`

