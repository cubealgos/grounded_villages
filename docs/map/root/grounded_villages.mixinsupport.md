<!-- generated by tools/map.py; edit the source, then run `just map` -->

# `grounded_villages.mixinsupport` — root

Every type with its summary and every non-private constructor, method and constant. The
signature is the contract; read the source only when the summary is not enough.

### `class PieceLadderContext` — `src/main/java/grounded_villages/mixinsupport/PieceLadderContext.java`
Cross-mixin, per-thread state grounded_villages.mixin.village's per-child rejection (GV-7, `docs/spec/domains/pieces.md`) and shrink/move/vanilla ladder (`decisions/DEC-010-shrink-move-vanilla.md`) share, for the same reason every other context in this package exists: findGenerationPoint -> addPieces -> tryPlacingChildren runs synchronously on one worker thread per structure-generation call, but many such calls run concurrently across chunks/threads, and a plain support class cannot live inside the declared grounded_villages.mixin package (VillageTagContext's own javadoc has the IllegalClassLoadError this was moved to avoid).
- `void setBypass(boolean bypass)`
- `boolean isBypass()`
- `void setSuppressWrap(boolean suppress)`
- `boolean isSuppressWrap()`
- `void setSampler(TerrainSampler sampler)` — The one TerrainSampler PlacerMixin's own gv$prepare builds once per tryPlacingChildren call and every candidate in that call reuses; null when this call should not be evaluated at all (see PlacerMixin#gv$prepare's own javadoc for the three reasons).
- `TerrainSampler getSampler()`
- `void setCurrentCandidateRejected(boolean rejected)` — Set by the constructor-redirect for the candidate currently mid-commit on this thread.
- `boolean isCurrentCandidateRejected()` — false (accept) when nothing was ever set -- fails open, never silently rejects.
- `void setRetryHandle(RetryHandle handle)`
- `RetryHandle getRetryHandle()`
- `void beginAttempt()`
- `Accumulator endAttempt()` — Ends the current attempt, returning what it accumulated (never null).
- `void recordAccept(boolean street)` — A no-op outside an open attempt (e.g.
- `void recordReject(boolean street, grounded_villages.piece.PieceRejectionReason reason)`

    - **nested** `class Accumulator`
    - `int totalRejected()`

    - **nested** `record RetryHandle(Structure.GenerationContext context, int radius, Function<BlockPos, Optional<Structure.GenerationStub>> retry)`
    vanilla-fallback) shares -- one seeded random(), one chunk position SiteSearch#searchAlternative} needs for the "move" step with every other parameter fixed to what this village candidate actually rolled -- built once, while those parameters are all still in scope as gv$onAddPieces's own method parameters

### `class StartHeightContext` — `src/main/java/grounded_villages/mixinsupport/StartHeightContext.java`
The village's own "start height" (`03-glossary.md`: "the one ground height a village's site is evaluated and accepted at; every piece's own height-deviation check is measured against this single reference, not against each piece's own local terrain") -- set once per village candidate by grounded_villages.mixin.village.JigsawPlacementMixin's own gv$decideStart, right after it settles on the final X/Z (kept, or shifted by SiteStartHook), read by grounded_villages.piece.PieceRejectionHook for every child candidate `PlacerMixin` evaluates afterwards on the same thread.
- `void set(Integer startHeight)`
- `Integer get()` — null when no village candidate is currently being decided on this thread.

### `class TierAssignmentContext` — `src/main/java/grounded_villages/mixinsupport/TierAssignmentContext.java`
Bridges the tier roll JigsawStructureMixin makes once per village candidate (GV-8, `docs/spec/domains/tiers.md`) to JigsawPlacementMixin's write-back of maxDepth/ max_distance_from_center one call frame down -- the same cross-mixin-class handoff shape VillageTagContext already uses for the #minecraft:village tag gate, and for the same reason: Structure.GenerationContext's own fields carry no reference back to the tier roll made against it (`VillageTagContext`'s own javadoc explains the underlying javap finding this mirrors).
- `void set(TierAssignment assignment)`
- `TierAssignment get()` — null when no roll happened for this thread's current structure call.

### `class VillageTagContext` — `src/main/java/grounded_villages/mixinsupport/VillageTagContext.java`
Bridges JigsawStructureMixin's #minecraft:village tag test (run once per JigsawStructure.findGenerationPoint call -- the only place in the vanilla call chain with a this to test against the structure registry, decisions DEC-007-village-tag-scope.md) to JigsawPlacementMixin and PlacerMixin, both static methods one level down whose own parameters carry no structure reference back to the Structure/Holder that owns them (Structure.GenerationContext's record fields, confirmed by javap, GV-5).
- `void set(boolean isVillage)`
- `boolean isVillage()`

